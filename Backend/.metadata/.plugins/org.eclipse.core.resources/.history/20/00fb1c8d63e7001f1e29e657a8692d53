package com.proyecto.fabrica.ps.mapper;

import org.mapstruct.Mapper;

import com.proyecto.fabrica.ps.dto.MovementWorkerDTO;
import com.proyecto.fabrica.ps.models.MovementWorker;

@Mapper(componentModel = "spring")
public interface MovementWorkerMapper {
	
	MovementWorkerDTO movementWorkerDTO(MovementWorker movementWorker);
}
